name: go_router_test
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: 3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 1.0.4
  mocktail: 0.2.0

flutter:
  uses-material-design: true
